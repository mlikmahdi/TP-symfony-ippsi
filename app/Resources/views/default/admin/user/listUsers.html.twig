{% extends 'base.html.twig' %}
{% block title %}Users{% endblock %}

{% block body %}
    <h1>Users</h1>
    <hr>
    <a class="btn btn-light mb-3" href="{{ path('adminHome') }}">Dashboard</a>
    <div class="row">
        {% for user in users %}
            <div class="col-sm-6 col-md-4">
                <div class="card mb-3" style="width: 18rem;">
                    <div class="card-body">
                        <h3 class="card-title">{{ user.username }}</h3>
                        <h4 class="card-text">{{ user.email }}</h4>
                        <a href="{{ path('userAdmin_view', {'id' : user.id}) }}" class="btn btn-outline-secondary">Détails de l'utilisateur</a>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>

{% endblock %}
